# Linking awos tests to packages {#sec-linking-awos-tests-to-packages}

You can link awos module tests to the packages that they exercised,
so that the tests can be run automatically during code review when the package gets changed.
This is
[described in the nixpkgs manual](https://awos.org/manual/nixpkgs/stable/#ssec-awos-tests-linking).
